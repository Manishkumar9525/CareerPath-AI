{
  "name": "careerpath-ai",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "client": "npm run dev --prefix frontend",
    "server": "npm run dev --prefix backend",
    "dev": "concurrently -n \" SERVER, CLIENT\" -c \"bgRed.bold,bgBlue.bold\" \"npm run server\" \"npm run client\""
  }
}